- OHCO Structure (as delimitted column names): token_str, term_str, pos, pos_group, token_str, term_str
- Columns (as delimitted column names, including `token_str`, `term_str`, `pos`, and `pos_group`): token_str and term_str are the multiindex; pos, pos_group, token_str, and term_str are the columns  
^^ are these columns okay?

Is ngram_length in vocab needed? How do I add?

What is DTM, is it different from DTCM?

For TFIDF- L2 norm:
Number of features (i.e. significant words):
Principle of significant word selection: 
^ How do I calculate these? Is my approach reasonable? (Detailed in FinalProject.ipynb)

I'd love help interpreting my PC table

Is my TFIDF table complete? What do you mean by description of formula? I attempted but not sure if right

Thank you!
